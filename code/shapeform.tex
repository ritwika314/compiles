Squareform(pid)

using Motion:
...

allread:
  pos p$\label{shapeformp}$

local:
  List<List<int>> nbrs = ...
  List<int> corners = ...
  

Updatepos:
  pre True
  eff :
     if not(pid in corners) :
        Motion.target = midpoint(get(nbrs,pid))
    p[pid] = Motion.psn

    